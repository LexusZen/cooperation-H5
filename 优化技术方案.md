# 深汕特别合作区 H5 项目 - 优化技术方案

> 版本：v2.0
> 日期：2025年
> 状态：待实施

---

## 📋 优化需求概览

### 需求分类

| 优先级 | 板块 | 优化类型 | 复杂度 | 预估工时 |
|--------|------|---------|--------|---------|
| P0 | 板块1 | 开屏动画重构 | ⭐⭐⭐⭐⭐ | 8h |
| P0 | 板块2 | 时间轴布局改造 | ⭐⭐⭐⭐ | 6h |
| P0 | 板块3 | 柱状图重构 | ⭐⭐⭐⭐⭐ | 8h |
| P1 | 板块4 | 图文结构调整 | ⭐⭐⭐ | 4h |
| P1 | 全局 | 文字打字机效果 | ⭐⭐⭐⭐ | 6h |
| P1 | 全局 | 小标题汽车轮廓样式 | ⭐⭐⭐ | 4h |

**总预估工时：36小时**

---

## 🎯 优化需求详解

### 1. 板块1：开屏动画全新设计 ⭐⭐⭐⭐⭐

#### 1.1 需求描述

**当前问题：**
- 进入板块后直接展示所有内容
- 缺少沉浸式的开屏引导体验
- 三张产业地标图片轮播位置不突出

**新设计要求：**

**阶段1：全景背景展示（0-3秒）**
- 渲染完整的深汕全景航拍背景图
- 添加动态光影效果（从左到右扫过）
- 中心显示"深汕特别合作区"文字淡入
- 底部显示"加载中..."进度条动画

**阶段2：聚焦轮播展示（3-12秒）**
- 背景图虚化（blur）
- 屏幕中心出现大屏轮播容器（占屏幕70%）
- 自动播放三张产业地标图片
- 每张图片停留3秒，配合文字标题
- 轮播效果：3D翻转或立方体切换

**阶段3：缩小过渡（12-14秒）**
- 轮播容器缩小至屏幕上方（30%高度）
- 背景恢复清晰
- 底部淡入"进入页面"按钮（带呼吸动画）

**阶段4：进入内容（点击后）**
- 轮播容器和按钮淡出
- 向上滑动展示板块1的小标题列表
- 列表项逐行淡入（打字机效果）

#### 1.2 技术实现方案

##### 1.2.1 组件结构重构

```vue
<template>
  <div class="section section-1" ref="sectionRef">
    <!-- 背景图层 -->
    <div class="bg-layer" :class="{ blurred: currentStage >= 2 }">
      <div class="light-sweep" v-if="currentStage === 1"></div>
    </div>

    <!-- 阶段1：全屏欢迎 -->
    <transition name="fade">
      <div v-if="currentStage === 1" class="welcome-screen">
        <h1 class="welcome-title">深汕特别合作区</h1>
        <div class="loading-bar">
          <div class="progress" :style="{ width: loadingProgress + '%' }"></div>
        </div>
      </div>
    </transition>

    <!-- 阶段2-3：聚焦轮播 -->
    <transition name="scale">
      <div v-if="currentStage === 2 || currentStage === 3"
           class="carousel-spotlight"
           :class="{ shrunk: currentStage === 3 }">
        <swiper
          :modules="[Autoplay, EffectCube]"
          :effect="'cube'"
          :autoplay="{ delay: 3000 }"
          :cube-effect="{ shadow: true, slideShadows: true }"
          @slide-change="onSlideChange"
          class="spotlight-swiper"
        >
          <swiper-slide v-for="(img, index) in landmarkImages" :key="index">
            <div class="spotlight-item">
              <img :src="img.src" :alt="img.label" />
              <div class="spotlight-label">{{ img.label }}</div>
            </div>
          </swiper-slide>
        </swiper>
      </div>
    </transition>

    <!-- 阶段3：进入按钮 -->
    <transition name="breathe">
      <button v-if="currentStage === 3"
              class="enter-button"
              @click="enterContent">
        <span class="button-text">进入页面</span>
        <span class="button-icon">→</span>
      </button>
    </transition>

    <!-- 阶段4：内容展示 -->
    <transition name="slide-up">
      <div v-if="currentStage === 4" class="content">
        <h2 class="section-title">山海新城，产业高地</h2>

        <!-- 小标题列表（打字机效果） -->
        <div class="highlights-section">
          <div class="highlights-list">
            <div
              v-for="(item, index) in highlights"
              :key="index"
              class="highlight-item"
              :style="{
                animationDelay: (index * 0.3) + 's',
                display: showHighlight >= index ? 'flex' : 'none'
              }"
            >
              <div class="car-outline-bg">
                <div class="dot"></div>
                <div class="text typewriter">{{ displayTexts[index] }}</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </transition>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted, computed } from 'vue'
import { Swiper, SwiperSlide } from 'swiper/vue'
import { Autoplay, EffectCube } from 'swiper/modules'
import { useAnimation } from '@/composables/useAnimation'
import { useTypewriter } from '@/composables/useTypewriter' // 新增

// 阶段控制：1=欢迎屏 2=大屏轮播 3=缩小+按钮 4=内容展示
const currentStage = ref(1)
const loadingProgress = ref(0)
const showHighlight = ref(-1)
const displayTexts = ref<string[]>([])

const { createTimeline } = useAnimation()
const { typeText } = useTypewriter()

// 产业地标图片
const landmarkImages = [
  { src: '/images/section1/landmark-1.jpg', label: '比亚迪深圳号 - 出厂即出海' },
  { src: '/images/section1/landmark-2.jpg', label: '深汕比亚迪二期 - 高速建设' },
  { src: '/images/section1/landmark-3.jpg', label: '小漠国际物流港 - 5分钟极速集港' }
]

// 小标题数据
const highlights = [
  '规模以上工业增加值增长52%',
  '汽车制造业增加值增长18977.1%',
  '2024年：地区生产总值242.15亿元，同比增长74.2%',
  // ... 其他数据
]

onMounted(() => {
  startOpeningSequence()
})

// 开屏动画序列
const startOpeningSequence = () => {
  // 阶段1：欢迎屏（0-3秒）
  animateLoading()

  setTimeout(() => {
    currentStage.value = 2 // 进入大屏轮播
  }, 3000)

  // 阶段2→3：轮播3张图后缩小（3-12秒）
  setTimeout(() => {
    currentStage.value = 3 // 缩小+显示按钮
  }, 12000)
}

// 加载进度动画
const animateLoading = () => {
  const interval = setInterval(() => {
    loadingProgress.value += 2
    if (loadingProgress.value >= 100) {
      clearInterval(interval)
    }
  }, 60) // 3秒加载完成
}

// 进入内容
const enterContent = () => {
  currentStage.value = 4

  // 逐行显示小标题（打字机效果）
  highlights.forEach((text, index) => {
    setTimeout(() => {
      showHighlight.value = index
      typeText(text, (displayText) => {
        displayTexts.value[index] = displayText
      })
    }, index * 800) // 每行间隔800ms
  })
}
</script>

<style scoped lang="scss">
// 背景层
.bg-layer {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: url('/images/section1/bg-main.jpg');
  background-size: cover;
  background-position: center;
  transition: filter 0.8s ease;

  &.blurred {
    filter: blur(10px);
  }

  // 光影扫过效果
  .light-sweep {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    animation: sweepLight 3s ease-out;
  }
}

@keyframes sweepLight {
  0% { left: -100%; }
  100% { left: 100%; }
}

// 欢迎屏
.welcome-screen {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  z-index: 10;

  .welcome-title {
    font-size: clamp(32px, 6vw, 56px);
    font-weight: bold;
    color: white;
    text-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
    margin-bottom: 40px;
    animation: titleFadeIn 1.5s ease;
  }

  .loading-bar {
    width: 300px;
    height: 6px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 3px;
    overflow: hidden;

    .progress {
      height: 100%;
      background: linear-gradient(90deg, #3b82f6, #60a5fa);
      transition: width 0.1s linear;
    }
  }
}

// 聚焦轮播
.carousel-spotlight {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 70vw;
  height: 60vh;
  z-index: 10;
  transition: all 1s cubic-bezier(0.34, 1.56, 0.64, 1);

  &.shrunk {
    top: 20%;
    width: 80vw;
    height: 30vh;
  }

  .spotlight-swiper {
    width: 100%;
    height: 100%;
    border-radius: 20px;
    box-shadow: 0 20px 80px rgba(0, 0, 0, 0.5);

    .spotlight-item {
      position: relative;
      width: 100%;
      height: 100%;

      img {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }

      .spotlight-label {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        background: linear-gradient(to top, rgba(0, 0, 0, 0.9), transparent);
        padding: 30px;
        color: white;
        font-size: clamp(16px, 3vw, 24px);
        font-weight: bold;
        text-align: center;
      }
    }
  }
}

// 进入按钮
.enter-button {
  position: absolute;
  bottom: 15%;
  left: 50%;
  transform: translateX(-50%);
  padding: 18px 48px;
  background: linear-gradient(135deg, #3b82f6, #1e40af);
  color: white;
  font-size: 20px;
  font-weight: bold;
  border: none;
  border-radius: 50px;
  cursor: pointer;
  box-shadow: 0 8px 30px rgba(59, 130, 246, 0.5);
  display: flex;
  align-items: center;
  gap: 12px;
  animation: breathe 2s ease-in-out infinite;
  transition: all 0.3s ease;

  &:hover {
    transform: translateX(-50%) scale(1.1);
    box-shadow: 0 12px 40px rgba(59, 130, 246, 0.7);
  }

  .button-icon {
    font-size: 24px;
    transition: transform 0.3s ease;
  }

  &:hover .button-icon {
    transform: translateX(5px);
  }
}

@keyframes breathe {
  0%, 100% {
    box-shadow: 0 8px 30px rgba(59, 130, 246, 0.5);
  }
  50% {
    box-shadow: 0 8px 50px rgba(59, 130, 246, 0.8), 0 0 30px rgba(59, 130, 246, 0.6);
  }
}

// 内容区域
.content {
  position: relative;
  z-index: 1;
  padding: 40px 20px;
  animation: slideUp 0.8s ease;
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(50px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

// 汽车轮廓背景（见全局样式部分）
.car-outline-bg {
  // 详见后续全局样式
}
</style>
```

##### 1.2.2 关键技术点

1. **Swiper 3D Cube 效果**
   - 使用 `effect="cube"` 实现立体翻转
   - 配置 `slideShadows: true` 增强立体感

2. **多阶段状态管理**
   - 使用 `currentStage` 控制4个阶段
   - 结合 `setTimeout` 和 Vue Transition 实现流畅过渡

3. **动态加载进度条**
   - 使用 `setInterval` 模拟加载进度
   - 宽度动画通过绑定 `:style` 实现

4. **背景虚化**
   - 使用 CSS `filter: blur(10px)` 实现
   - 配合 `transition` 平滑过渡

---

### 2. 板块2：竖向时间轴重构 ⭐⭐⭐⭐

#### 2.1 需求描述

**当前问题：**
- 水平时间轴在手机上需要横向滚动
- 图片过大，无法在一屏内完整展示
- 柱状图占用空间过多

**新设计要求：**
- 改为**竖向时间轴**（无论PC还是手机）
- 时间节点图片**缩小至卡片大小**（宽度占屏幕70%）
- 确保手机端**一屏完整展示**时间轴内容
- 去除当前的分组柱状图
- 保留重点项目展示

#### 2.2 技术实现方案

##### 2.2.1 布局结构改造

```vue
<template>
  <div class="section section-2" ref="sectionRef">
    <div class="content">
      <h2 class="section-title typewriter">深汕速度，跨越发展</h2>

      <!-- 竖向时间轴 -->
      <div class="timeline-vertical">
        <!-- 中心连接线 -->
        <div class="timeline-line"></div>

        <!-- 时间节点 -->
        <div
          class="timeline-node"
          v-for="(item, index) in timelineData"
          :key="index"
          :class="{ 'node-left': index % 2 === 0, 'node-right': index % 2 === 1, active: visibleNodes >= index }"
        >
          <!-- 年份标签 -->
          <div class="year-badge">{{ item.year }}</div>

          <!-- 中心圆点 -->
          <div class="timeline-dot"></div>

          <!-- 内容卡片 -->
          <div class="timeline-card">
            <div class="card-image">
              <img :src="item.image" :alt="item.year" />
            </div>
            <div class="card-content">
              <h4>{{ item.title }}</h4>
              <p>{{ item.desc }}</p>
            </div>
          </div>
        </div>
      </div>

      <!-- 重点项目展示（保留） -->
      <div class="key-projects">
        <!-- 原有代码... -->
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useIntersection } from '@/composables/useIntersection'

const sectionRef = ref<HTMLElement | null>(null)
const visibleNodes = ref(-1)

// 时间轴数据
const timelineData = [
  {
    year: '2022年4月',
    title: '起步阶段',
    desc: '深汕合作区启动基础设施建设，招商引资全面展开',
    image: '/images/section2/timeline-2022.jpg'
  },
  {
    year: '2023年5月',
    title: '加速发展',
    desc: '比亚迪二期项目启动，从动工到投产仅10个月',
    image: '/images/section2/timeline-2023.jpg'
  },
  {
    year: '2025年5月',
    title: '跨越突破',
    desc: '四期电池包项目从签约到投产仅半年，深汕速度创新高',
    image: '/images/section2/timeline-2025.jpg'
  }
]

// 监听可见性，逐个显示节点
useIntersection(sectionRef, () => {
  timelineData.forEach((_, index) => {
    setTimeout(() => {
      visibleNodes.value = index
    }, index * 600)
  })
})
</script>

<style scoped lang="scss">
.section-2 {
  width: 100%;
  min-height: 100vh;
  background: linear-gradient(to bottom, #ffffff, #e0f2fe);
  padding: 60px 20px;

  .content {
    max-width: 1000px;
    margin: 0 auto;
  }

  .section-title {
    font-size: clamp(28px, 5vw, 42px);
    font-weight: bold;
    text-align: center;
    color: rgb(22, 93, 255);
    margin-bottom: 60px;
  }

  // 竖向时间轴
  .timeline-vertical {
    position: relative;
    padding: 40px 0;

    // 中心连接线
    .timeline-line {
      position: absolute;
      left: 50%;
      top: 0;
      bottom: 0;
      width: 4px;
      background: linear-gradient(to bottom, #3b82f6, #93c5fd);
      transform: translateX(-50%);

      @media (max-width: 768px) {
        left: 30px;
      }
    }

    // 时间节点
    .timeline-node {
      position: relative;
      margin-bottom: 60px;
      opacity: 0;
      transform: scale(0.8);
      transition: all 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);

      &.active {
        opacity: 1;
        transform: scale(1);
      }

      // 年份标签
      .year-badge {
        position: absolute;
        top: 0;
        background: linear-gradient(135deg, #3b82f6, #1e40af);
        color: white;
        padding: 10px 24px;
        border-radius: 30px;
        font-size: 16px;
        font-weight: bold;
        box-shadow: 0 4px 15px rgba(59, 130, 246, 0.4);
        white-space: nowrap;
        z-index: 2;
      }

      // 中心圆点
      .timeline-dot {
        position: absolute;
        top: 8px;
        width: 20px;
        height: 20px;
        background: #fbbf24;
        border: 4px solid white;
        border-radius: 50%;
        box-shadow: 0 0 0 4px rgba(251, 191, 36, 0.3), 0 0 20px rgba(251, 191, 36, 0.5);
        z-index: 3;
        animation: pulse 2s ease-in-out infinite;
      }

      // 内容卡片
      .timeline-card {
        width: 70%;
        max-width: 400px;
        background: white;
        border-radius: 16px;
        overflow: hidden;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
        transition: all 0.3s ease;

        &:hover {
          transform: translateY(-5px);
          box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
        }

        .card-image {
          width: 100%;
          height: 180px;
          overflow: hidden;

          img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
          }

          &:hover img {
            transform: scale(1.1);
          }
        }

        .card-content {
          padding: 20px;

          h4 {
            font-size: 18px;
            font-weight: bold;
            color: #1f2937;
            margin-bottom: 10px;
          }

          p {
            font-size: 14px;
            color: #6b7280;
            line-height: 1.6;
          }
        }
      }

      // 左侧节点
      &.node-left {
        display: flex;
        justify-content: flex-end;
        padding-right: calc(50% + 40px);

        .year-badge {
          right: calc(50% + 60px);
        }

        .timeline-dot {
          left: calc(50% - 10px);
        }

        @media (max-width: 768px) {
          justify-content: flex-start;
          padding-right: 0;
          padding-left: 70px;

          .year-badge {
            left: 70px;
            right: auto;
          }

          .timeline-dot {
            left: 20px;
          }
        }
      }

      // 右侧节点
      &.node-right {
        display: flex;
        justify-content: flex-start;
        padding-left: calc(50% + 40px);

        .year-badge {
          left: calc(50% + 60px);
        }

        .timeline-dot {
          right: calc(50% - 10px);
        }

        @media (max-width: 768px) {
          padding-left: 70px;

          .year-badge {
            left: 70px;
          }

          .timeline-dot {
            left: 20px;
            right: auto;
          }
        }
      }
    }
  }

  // 重点项目展示（保留原样式）
  .key-projects {
    margin-top: 60px;
    // ... 原有样式
  }
}

@keyframes pulse {
  0%, 100% {
    transform: scale(1);
  }
  50% {
    transform: scale(1.2);
    box-shadow: 0 0 0 6px rgba(251, 191, 36, 0.4), 0 0 30px rgba(251, 191, 36, 0.6);
  }
}
</style>
```

##### 2.2.2 关键技术点

1. **竖向时间轴布局**
   - PC端：左右交替布局（zigzag）
   - 移动端：统一左对齐，缩进显示

2. **图片尺寸控制**
   - 卡片宽度限制为 `70%` 或 `max-width: 400px`
   - 图片高度固定 `180px`，使用 `object-fit: cover`

3. **响应式适配**
   - 使用媒体查询 `@media (max-width: 768px)` 调整布局
   - 时间线从中心移至左侧 30px 位置

4. **动画优化**
   - 节点逐个淡入（stagger effect）
   - 圆点脉冲动画增强视觉焦点

---

### 3. 板块3：柱状图数据重构 ⭐⭐⭐⭐⭐

#### 3.1 需求描述

**当前问题：**
- 柱状图展示的是综合经济数据（工业产值、出口额等）
- 布局占用空间过大
- 小标题独立展示，未与柱状图关联
- 缺少逐条展示的动画效果

**新设计要求：**

**数据结构调整：**
- 展示**三个独立的柱状图**
- 每个柱状图显示 **2023-2025年** 的数据
- 三个指标：
  1. **汽车年生产量**（万辆）
  2. **高科技企业数量**（家）
  3. **汽车出口量**（万辆）

**布局要求：**
- 每个柱状图为一个独立模块
- 左侧：3年份的柱状图（占一半宽度）
- 右侧：配套文字描述
- 三个模块纵向排列，刚好占满一个手机屏幕
- 去除"产业发展亮点"底部内容

**交互要求：**
- 柱状图一条条展示（GSAP stagger）
- 点击柱子弹出小弹窗介绍
- 文字采用打字机效果逐行显示

#### 3.2 技术实现方案

##### 3.2.1 数据结构定义

```typescript
// src/types/index.ts 新增
export interface ChartDataItem {
  year: string
  value: number
  color: string
  detail: string // 点击后显示的详细说明
}

export interface ChartModule {
  id: string
  title: string
  unit: string
  description: string
  data: ChartDataItem[]
}
```

##### 3.2.2 组件重构

```vue
<template>
  <div class="section section-3" ref="sectionRef">
    <div class="bg-image"></div>

    <div class="content">
      <h2 class="section-title typewriter">深汕智造，通达全球</h2>

      <!-- 三个柱状图模块 -->
      <div class="chart-modules">
        <div
          v-for="(module, moduleIndex) in chartModules"
          :key="module.id"
          class="chart-module"
          :class="{ visible: visibleModules >= moduleIndex }"
        >
          <!-- 左侧：柱状图 -->
          <div class="chart-container">
            <h3 class="chart-title">{{ module.title }}</h3>

            <div class="chart-bars">
              <div
                v-for="(item, barIndex) in module.data"
                :key="item.year"
                class="bar-item"
                :class="{ 'bar-visible': visibleBars[moduleIndex] >= barIndex }"
                @click="showBarDetail(item)"
              >
                <div class="bar-wrapper">
                  <div
                    class="bar"
                    :style="{
                      height: (item.value / getMaxValue(module.data) * 100) + '%',
                      background: item.color
                    }"
                  >
                    <span class="bar-value">{{ item.value }}{{ module.unit }}</span>
                  </div>
                </div>
                <div class="bar-label">{{ item.year }}</div>
              </div>
            </div>
          </div>

          <!-- 右侧：文字描述 -->
          <div class="description-container">
            <p class="description-text typewriter">{{ module.description }}</p>
          </div>
        </div>
      </div>
    </div>

    <!-- 点击柱子弹出的弹窗 -->
    <transition name="fade">
      <div v-if="activeBar" class="bar-tooltip" @click="activeBar = null">
        <div class="tooltip-content" @click.stop>
          <button class="close-btn" @click="activeBar = null">×</button>
          <h4>{{ activeBar.year }}</h4>
          <p class="tooltip-value">{{ activeBar.value }}{{ activeBar.unit }}</p>
          <p class="tooltip-detail">{{ activeBar.detail }}</p>
        </div>
      </div>
    </transition>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import { useIntersection } from '@/composables/useIntersection'
import { useAnimation } from '@/composables/useAnimation'
import type { ChartModule, ChartDataItem } from '@/types'

const sectionRef = ref<HTMLElement | null>(null)
const visibleModules = ref(-1)
const visibleBars = ref<number[]>([0, 0, 0])
const activeBar = ref<any>(null)

const { createTimeline } = useAnimation()

// 三个柱状图模块数据
const chartModules: ChartModule[] = [
  {
    id: 'car-production',
    title: '汽车年生产量',
    unit: '万辆',
    description: '深汕合作区依托比亚迪等龙头企业，汽车产量实现爆发式增长。2023年达100万辆，2024年突破130万辆，2025年预计达150万辆，成为全球新能源汽车制造重镇。',
    data: [
      {
        year: '2023',
        value: 100,
        color: 'linear-gradient(to top, #3b82f6, #60a5fa)',
        detail: '2023年是深汕汽车产业的起飞之年，比亚迪深圳号工厂全面投产，实现年产100万辆的里程碑。'
      },
      {
        year: '2024',
        value: 130,
        color: 'linear-gradient(to top, #10b981, #34d399)',
        detail: '2024年产量同比增长30%，达到130万辆。比亚迪二期项目投产，汽车制造业增加值增长621.1%。'
      },
      {
        year: '2025',
        value: 150,
        color: 'linear-gradient(to top, #f59e0b, #fbbf24)',
        detail: '2025年预计产量达150万辆，全球首家达成第1300万辆新能源汽车下线里程碑的工厂诞生于此。'
      }
    ]
  },
  {
    id: 'tech-companies',
    title: '高科技企业数量',
    unit: '家',
    description: '围绕新能源汽车产业链，深汕合作区吸引了速腾聚创、壁虎科技等近30家创新型企业入驻，形成完整的产业生态链，企业数量年均增长40%以上。',
    data: [
      {
        year: '2023',
        value: 120,
        color: 'linear-gradient(to top, #8b5cf6, #a78bfa)',
        detail: '2023年已有120家规模以上工业企业落户，涵盖整车制造、零部件、智能驾驶等领域。'
      },
      {
        year: '2024',
        value: 180,
        color: 'linear-gradient(to top, #ec4899, #f472b6)',
        detail: '2024年企业数量突破180家，同比增长50%。产业集群效应显现，配套产业链日趋完善。'
      },
      {
        year: '2025',
        value: 250,
        color: 'linear-gradient(to top, #f59e0b, #fbbf24)',
        detail: '2025年预计达250家，深汕成为粤港澳大湾区新能源汽车产业的核心承载地之一。'
      }
    ]
  },
  {
    id: 'car-exports',
    title: '汽车出口量',
    unit: '万辆',
    description: '依托小漠国际物流港，深汕实现"出厂即出海"。开通11条国际航线,产品远销欧洲、南美、东南亚、非洲等70多个国家和地区，出口量年均增长超过100%。',
    data: [
      {
        year: '2023',
        value: 30,
        color: 'linear-gradient(to top, #06b6d4, #22d3ee)',
        detail: '2023年出口量达30万辆，主要出口至东南亚和非洲市场，出口增速全国领先。'
      },
      {
        year: '2024',
        value: 65,
        color: 'linear-gradient(to top, #10b981, #34d399)',
        detail: '2024年出口量翻倍增长至65万辆，欧洲和南美市场实现突破，5分钟极速集港效率全球领先。'
      },
      {
        year: '2025',
        value: 90,
        color: 'linear-gradient(to top, #f59e0b, #fbbf24)',
        detail: '2025年出口量预计达90万辆，产品出口至澳大利亚最快仅需10天抵达，国际化水平持续提升。'
      }
    ]
  }
]

// 计算每个模块的最大值
const getMaxValue = (data: ChartDataItem[]) => {
  return Math.max(...data.map(item => item.value))
}

// 显示柱子详情
const showBarDetail = (bar: ChartDataItem) => {
  activeBar.value = { ...bar, unit: chartModules[0].unit } // 补充单位信息
}

// 监听可见性，逐模块显示
useIntersection(sectionRef, () => {
  const tl = createTimeline()

  // 逐个模块显示
  chartModules.forEach((_, moduleIndex) => {
    tl.add(() => {
      visibleModules.value = moduleIndex

      // 该模块的柱子逐条显示
      setTimeout(() => {
        animateBars(moduleIndex)
      }, 300)
    }, `+=${moduleIndex === 0 ? 0.5 : 1}`)
  })
})

// 柱子逐条动画
const animateBars = (moduleIndex: number) => {
  chartModules[moduleIndex].data.forEach((_, barIndex) => {
    setTimeout(() => {
      visibleBars.value[moduleIndex] = barIndex
    }, barIndex * 400) // 每条柱子间隔400ms
  })
}
</script>

<style scoped lang="scss">
.section-3 {
  position: relative;
  width: 100%;
  min-height: 100vh;
  color: white;
  padding: 60px 20px;

  .bg-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('/images/section3/bg-industry.jpg');
    background-size: cover;
    background-position: center;
    z-index: 0;

    &::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.4);
    }
  }

  .content {
    position: relative;
    z-index: 1;
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-title {
    font-size: clamp(28px, 5vw, 42px);
    font-weight: bold;
    text-align: center;
    color: white;
    margin-bottom: 40px;
    text-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
  }

  // 柱状图模块容器
  .chart-modules {
    display: flex;
    flex-direction: column;
    gap: 30px;

    // 单个模块
    .chart-module {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
      background: rgba(255, 255, 255, 0.95);
      border-radius: 20px;
      padding: 30px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
      opacity: 0;
      transform: translateY(30px);
      transition: all 0.8s ease;

      &.visible {
        opacity: 1;
        transform: translateY(0);
      }

      @media (max-width: 768px) {
        grid-template-columns: 1fr;
        padding: 20px;
        gap: 20px;
      }

      // 左侧：柱状图
      .chart-container {
        display: flex;
        flex-direction: column;

        .chart-title {
          font-size: clamp(18px, 3vw, 22px);
          font-weight: bold;
          color: rgb(22, 93, 255);
          margin-bottom: 20px;
          text-align: center;
        }

        .chart-bars {
          display: flex;
          justify-content: space-around;
          align-items: flex-end;
          height: 200px;
          gap: 15px;
          padding: 0 20px;
          border-bottom: 3px solid #e5e7eb;
          position: relative;

          @media (max-width: 768px) {
            height: 180px;
            gap: 10px;
          }

          .bar-item {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            cursor: pointer;
            opacity: 0;
            transform: scaleY(0);
            transform-origin: bottom;
            transition: all 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);

            &.bar-visible {
              opacity: 1;
              transform: scaleY(1);
            }

            &:hover {
              transform: scaleY(1.05) translateY(-5px);

              .bar {
                box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
              }
            }

            .bar-wrapper {
              flex: 1;
              width: 100%;
              display: flex;
              align-items: flex-end;
              justify-content: center;

              .bar {
                width: 100%;
                max-width: 60px;
                min-height: 30px;
                border-radius: 8px 8px 0 0;
                display: flex;
                align-items: flex-start;
                justify-content: center;
                padding-top: 10px;
                box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2);
                position: relative;

                &::before {
                  content: '';
                  position: absolute;
                  top: 0;
                  left: 0;
                  right: 0;
                  height: 4px;
                  background: rgba(255, 255, 255, 0.6);
                  border-radius: 8px 8px 0 0;
                }

                .bar-value {
                  font-size: clamp(11px, 2vw, 14px);
                  font-weight: bold;
                  color: white;
                  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
                }
              }
            }

            .bar-label {
              margin-top: 10px;
              font-size: 14px;
              font-weight: 600;
              color: #1f2937;
            }
          }
        }
      }

      // 右侧：文字描述
      .description-container {
        display: flex;
        align-items: center;
        padding: 20px;
        background: rgba(22, 93, 255, 0.05);
        border-radius: 12px;
        border-left: 4px solid rgb(22, 93, 255);

        .description-text {
          font-size: clamp(13px, 2.5vw, 15px);
          color: #1f2937;
          line-height: 1.8;
          text-align: justify;
        }
      }
    }
  }

  // 弹窗样式
  .bar-tooltip {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.75);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;

    .tooltip-content {
      background: white;
      border-radius: 20px;
      padding: 40px;
      max-width: 500px;
      width: 90%;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
      position: relative;
      animation: slideUp 0.3s ease;

      .close-btn {
        position: absolute;
        top: 15px;
        right: 15px;
        width: 35px;
        height: 35px;
        border: none;
        background: rgba(22, 93, 255, 0.1);
        border-radius: 50%;
        font-size: 24px;
        color: rgb(22, 93, 255);
        cursor: pointer;
        transition: all 0.3s ease;

        &:hover {
          background: rgb(22, 93, 255);
          color: white;
          transform: rotate(90deg);
        }
      }

      h4 {
        font-size: 24px;
        font-weight: bold;
        color: rgb(22, 93, 255);
        margin-bottom: 15px;
      }

      .tooltip-value {
        font-size: 32px;
        font-weight: bold;
        color: #1f2937;
        margin-bottom: 15px;
      }

      .tooltip-detail {
        font-size: 16px;
        color: #4b5563;
        line-height: 1.8;
      }
    }
  }
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
</style>
```

##### 3.2.3 关键技术点

1. **三模块纵向布局**
   - 使用 `grid-template-columns: 1fr 1fr` 实现左右布局
   - 移动端改为 `grid-template-columns: 1fr` 纵向堆叠

2. **柱子逐条动画**
   - 使用 `transform: scaleY(0)` 初始状态
   - 配合 `transform-origin: bottom` 从底部生长
   - `setTimeout` 控制每条柱子的出现时机

3. **数据点击弹窗**
   - 全屏遮罩层 + 居中弹窗
   - 使用 `@click.stop` 阻止事件冒泡
   - 关闭按钮带旋转动画

4. **响应式适配**
   - 柱状图高度在移动端降至 180px
   - 柱子宽度使用 `max-width` 限制

---

### 4. 板块4：图文结构调整 ⭐⭐⭐

#### 4.1 需求描述

**当前问题：**
- 小标题独立展示在顶部
- 民生设施轮播图没有配图说明

**新设计要求：**
- 小标题不独立展示
- 每张民生设施图片的**下方显示小标题**
- 需要补充的图片URL（提醒用户）：
  - 深汕人民医院实景图
  - 深圳中学高中园实景图
  - 保障房片区实景图 
  - 深汕大道/高铁站实景图 "F:\BaiduNetdiskDownload\深汕项目H5\深汕H5资源\板块4交通设施 四个高铁各2-3张\广汕铁路\2025年9月\深汕特别合作区广汕铁路全景2025.9.22-2.jpg"

  总共需要六个图配文：
    '广汕高铁已经通车，深汕高铁加快建设（广州40分钟、深圳30分钟生活圈加速形成）',"F:\BaiduNetdiskDownload\深汕项目H5\深汕H5资源\板块4交通设施 四个高铁各2-3张\广汕铁路\2025年9月\深汕特别合作区广汕铁路全景2025.9.22-2.jpg"
  '深汕西高速改扩建全线通车',"F:\BaiduNetdiskDownload\深汕项目H5\深汕H5资源\板块4交通设施 四个高铁各2-3张\深汕西高速改扩建提升工程\2025年9月\深汕特别合作区深汕西高速改扩建提升工程航拍2025.9.20-10.jpg"
  '深圳中学高中园已招生，深职大校区2026年启用',"F:\BaiduNetdiskDownload\深汕项目H5\深汕H5资源\板块4 教育设施 两个校区各2-3\深汕高中园项目及二高深汕实验学校\2025年9月\深汕特别合作区深汕中学高中园项目2025.9.19-6.jpg"
  '超2万套保障房落地，安居梦照进现实（"双六十"住房保障体系）',"F:\BaiduNetdiskDownload\深汕项目H5\深汕H5资源\板块4 保障房 3\赤河广场及临邦里片区\2025年9月\深汕特别合作区赤河广场2025.9.20-2.jpg"
  '深汕人民医院2025年底建成，深圳品质医疗可及',"F:\BaiduNetdiskDownload\深汕项目H5\深汕H5资源\板块4医疗设施 3\深圳市深汕人民医院\2025年9月\深汕特别合作区深圳市深汕人民医院全景图2025.9.20-7.jpg"
  '"集成式改革"试点推进，治理效能持续提升'"F:\BaiduNetdiskDownload\深汕项目H5\深汕H5资源\板块4交通设施 四个高铁各2-3张\通港大道项目\2025年9月\深汕特别合作区通港大道项目全景2025.9.21-10.jpg"

#### 4.2 技术实现方案

##### 4.2.1 数据结构调整

```typescript
// 在 Section4.vue 中调整数据结构
const facilities = [
  {
    title: '深圳市深汕人民医院',
    subtitle: '深圳品质医疗可及 - 预计2025年底建成',
    image: '/images/section4/hospital.jpg', // ⚠️ 需补充
    caption: '深汕人民医院预计2025年底建成，引入深圳优质医疗资源',
    description: '...'
  },
  {
    title: '深圳中学高中园',
    subtitle: '优质教育资源落地 - 已于2024年开始招生',
    image: '/images/section4/school.jpg', // ⚠️ 需补充
    caption: '深圳中学高中园已招生，深职大校区2026年启用',
    description: '...'
  },
  {
    title: '赤河广场保障房片区',
    subtitle: '安居梦照进现实 - 超2万套保障房落地',
    image: '/images/section4/housing.jpg', // ⚠️ 需补充
    caption: '建立"双六十"住房保障体系，让深汕成为真正宜居之城',
    description: '...'
  }
]

const transportData = {
  title: '深汕交通路网',
  image: '/images/section4/transport.jpg', // ⚠️ 需补充
  caption: '广汕高铁已通车，深汕高铁加快建设，深汕西高速改扩建全线通车',
  description: '...'
}
```

##### 4.2.2 模板调整

```vue
<template>
  <div class="section section-4" ref="sectionRef">
    <div class="content">
      <h2 class="section-title typewriter">深圳品质，宜居宜业</h2>

      <!-- 移除独立的小标题列表 -->

      <!-- 民生设施轮播（图片+标题） -->
      <div class="carousel-container">
        <h3 class="carousel-title">民生设施展示</h3>
        <swiper
          :modules="carouselModules"
          :slides-per-view="1"
          :space-between="20"
          :loop="true"
          :autoplay="{ delay: 4000, disableOnInteraction: false }"
          :pagination="{ clickable: true }"
          class="facilities-swiper"
        >
          <swiper-slide v-for="(facility, index) in facilities" :key="index">
            <div class="facility-card" @click="showFacilityInfo(facility)">
              <!-- 图片 -->
              <div class="facility-image">
                <img :src="facility.image" :alt="facility.title" />
              </div>

              <!-- 标题（图片下方） -->
              <div class="facility-caption">
                <h4>{{ facility.title }}</h4>
                <p class="caption-text typewriter">{{ facility.caption }}</p>
              </div>
            </div>
          </swiper-slide>
        </swiper>
      </div>

      <!-- 交通路网（图片+标题） -->
      <div class="transport-container">
        <h3 class="transport-title">交通路网</h3>
        <div class="transport-card" @click="showTransportInfo">
          <!-- 图片 -->
          <div class="transport-image">
            <img :src="transportData.image" alt="深汕交通路网" />
          </div>

          <!-- 标题（图片下方） -->
          <div class="transport-caption">
            <h4>{{ transportData.title }}</h4>
            <p class="caption-text typewriter">{{ transportData.caption }}</p>
          </div>
        </div>
      </div>
    </div>

    <!-- 弹出层（保留原有代码） -->
  </div>
</template>

<style scoped lang="scss">
.facility-caption,
.transport-caption {
  background: linear-gradient(to bottom, rgba(255, 255, 255, 0.95), rgba(248, 250, 252, 0.95));
  backdrop-filter: blur(10px);
  padding: 20px;
  border-radius: 0 0 16px 16px;

  h4 {
    font-size: clamp(18px, 3vw, 22px);
    font-weight: bold;
    color: rgb(22, 93, 255);
    margin-bottom: 10px;
    text-align: center;
  }

  .caption-text {
    font-size: clamp(13px, 2.5vw, 15px);
    color: #4b5563;
    line-height: 1.6;
    text-align: center;
  }
}
</style>
```

##### 4.2.3 ⚠️ 图片URL提醒

请准备以下图片并放置于 `public/images/section4/` 目录：

```
public/images/section4/
├── hospital.jpg      # 深汕人民医院效果图或实景图
├── school.jpg        # 深圳中学高中园实景图
├── housing.jpg       # 保障房片区实景图或规划图
└── transport.jpg     # 深汕大道/广汕高铁站实景图
```

---

### 5. 全局优化：文字打字机效果 ⭐⭐⭐⭐

#### 5.1 需求描述

**要求：**
- 所有文字内容逐行展示
- 模拟手工输入的打字机效果
- 先展示完一个块，再向下展示另一个块
- 做好动画协调，避免同时出现多个打字机

#### 5.2 技术实现方案

##### 5.2.1 创建打字机组合式函数

```typescript
// src/composables/useTypewriter.ts
import { ref } from 'vue'

export interface TypewriterOptions {
  speed?: number // 打字速度（ms/字符）
  delay?: number // 开始前延迟（ms）
  cursor?: boolean // 是否显示光标
  onComplete?: () => void // 完成回调
}

export function useTypewriter() {
  const isTyping = ref(false)

  /**
   * 打字机效果
   * @param text 要显示的文本
   * @param callback 每次更新时的回调
   * @param options 配置选项
   */
  const typeText = (
    text: string,
    callback: (displayText: string) => void,
    options: TypewriterOptions = {}
  ) => {
    const {
      speed = 50,
      delay = 0,
      cursor = false,
      onComplete
    } = options

    isTyping.value = true
    let currentIndex = 0

    setTimeout(() => {
      const timer = setInterval(() => {
        if (currentIndex <= text.length) {
          const displayText = text.slice(0, currentIndex)
          callback(cursor && currentIndex < text.length ? displayText + '|' : displayText)
          currentIndex++
        } else {
          clearInterval(timer)
          isTyping.value = false
          onComplete?.()
        }
      }, speed)
    }, delay)
  }

  /**
   * 逐行打字机效果
   * @param lines 文本行数组
   * @param callback 每行更新时的回调
   * @param options 配置选项
   */
  const typeLines = (
    lines: string[],
    callback: (lineIndex: number, displayText: string, isComplete: boolean) => void,
    options: TypewriterOptions = {}
  ) => {
    const { speed = 50, delay = 0 } = options

    let currentLineIndex = 0

    const typeNextLine = () => {
      if (currentLineIndex >= lines.length) {
        return
      }

      const line = lines[currentLineIndex]
      const lineIndex = currentLineIndex

      typeText(
        line,
        (displayText) => {
          callback(lineIndex, displayText, false)
        },
        {
          speed,
          delay: currentLineIndex === 0 ? delay : 300, // 行间间隔300ms
          cursor: true,
          onComplete: () => {
            callback(lineIndex, line, true) // 完成时移除光标
            currentLineIndex++
            typeNextLine()
          }
        }
      )
    }

    typeNextLine()
  }

  return {
    typeText,
    typeLines,
    isTyping
  }
}
```

##### 5.2.2 在组件中使用

**示例：板块1小标题逐行打字**

```vue
<template>
  <div class="highlights-section">
    <div class="highlights-list">
      <div
        v-for="(item, index) in highlights"
        :key="index"
        class="highlight-item"
        v-show="displayTexts[index] !== undefined"
      >
        <div class="dot"></div>
        <div class="text">{{ displayTexts[index] }}</div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useTypewriter } from '@/composables/useTypewriter'

const displayTexts = ref<string[]>([])
const { typeLines } = useTypewriter()

const highlights = [
  '规模以上工业增加值增长52%',
  '汽车制造业增加值增长18977.1%',
  '2024年：地区生产总值242.15亿元，同比增长74.2%',
  // ...
]

onMounted(() => {
  // 逐行打字机效果
  typeLines(
    highlights,
    (lineIndex, displayText, isComplete) => {
      displayTexts.value[lineIndex] = displayText
    },
    {
      speed: 30, // 30ms/字符
      delay: 500 // 开始前延迟500ms
    }
  )
})
</script>
```

##### 5.2.3 全局打字机CSS

```scss
// src/assets/styles/animations.scss 新增

// 打字机光标闪烁
@keyframes cursorBlink {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0;
  }
}

// 打字机文本类
.typewriter {
  display: inline-block;
  overflow: hidden;
  white-space: pre-wrap; // 保留换行
  word-break: break-word;

  // 如果文本包含 | 符号，添加闪烁动画
  &:after {
    content: '';
    animation: cursorBlink 1s step-end infinite;
  }
}
```

##### 5.2.4 动画协调策略

**原则：串行播放，避免重叠**

```typescript
// 示例：板块3的动画协调
const playAnimationSequence = () => {
  const tl = createTimeline()

  // 1. 标题打字机（2秒）
  tl.add(() => {
    typeText(sectionTitle, ...)
  })

  // 2. 等待标题完成 + 延迟500ms
  tl.add(() => {
    // 第一个柱状图模块出现
    visibleModules.value = 0
    animateBars(0) // 柱子逐条动画（1.2秒）
  }, '+=2.5')

  // 3. 第一个模块文字打字机（3秒）
  tl.add(() => {
    typeText(chartModules[0].description, ...)
  }, '+=1.5')

  // 4. 第二个模块（重复2-3步骤）
  tl.add(() => {
    visibleModules.value = 1
    animateBars(1)
  }, '+=3.5')

  // ... 依此类推
}
```

---

### 6. 全局样式：汽车轮廓背景 ⭐⭐⭐

#### 6.1 需求描述

**要求：**
- 小标题背景设计成**汽车的轮廓**
- 添加更鲜艳的颜色和阴影
- 写成全局样式，方便复用

#### 6.2 技术实现方案

##### 6.2.1 汽车轮廓SVG设计

```scss
// src/assets/styles/global.scss 新增

// 汽车轮廓小标题背景（全局复用）
.car-outline-bg {
  position: relative;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.98) 0%, rgba(248, 250, 252, 0.95) 100%);
  backdrop-filter: blur(15px);
  border-radius: 16px;
  padding: 18px 24px;
  box-shadow:
    0 8px 24px rgba(0, 0, 0, 0.15),
    0 0 0 2px rgba(22, 93, 255, 0.1),
    inset 0 1px 0 rgba(255, 255, 255, 0.8);
  transition: all 0.3s ease;
  overflow: hidden;

  // 汽车轮廓装饰（使用伪元素 + clip-path）
  &::before {
    content: '';
    position: absolute;
    top: 50%;
    left: -30px;
    transform: translateY(-50%);
    width: 80px;
    height: 40px;
    background: linear-gradient(135deg, rgba(22, 93, 255, 0.2), rgba(59, 130, 246, 0.2));
    clip-path: polygon(
      0% 50%,
      10% 30%,
      30% 30%,
      40% 10%,
      60% 10%,
      70% 30%,
      90% 30%,
      100% 50%,
      100% 70%,
      90% 70%,
      90% 80%,
      80% 90%,
      75% 90%,
      70% 80%,
      30% 80%,
      25% 90%,
      20% 90%,
      10% 80%,
      10% 70%,
      0% 70%
    ); // 简化的汽车侧面轮廓
    opacity: 0.6;
  }

  // 右侧装饰轮廓
  &::after {
    content: '';
    position: absolute;
    top: 50%;
    right: -30px;
    transform: translateY(-50%) scaleX(-1); // 水平翻转
    width: 80px;
    height: 40px;
    background: linear-gradient(135deg, rgba(251, 191, 36, 0.2), rgba(245, 158, 11, 0.2));
    clip-path: polygon(
      0% 50%,
      10% 30%,
      30% 30%,
      40% 10%,
      60% 10%,
      70% 30%,
      90% 30%,
      100% 50%,
      100% 70%,
      90% 70%,
      90% 80%,
      80% 90%,
      75% 90%,
      70% 80%,
      30% 80%,
      25% 90%,
      20% 90%,
      10% 80%,
      10% 70%,
      0% 70%
    );
    opacity: 0.6;
  }

  // Hover 效果
  &:hover {
    transform: translateX(8px);
    box-shadow:
      0 12px 32px rgba(0, 0, 0, 0.2),
      0 0 0 3px rgba(22, 93, 255, 0.2),
      inset 0 1px 0 rgba(255, 255, 255, 1);
    background: linear-gradient(135deg, rgba(255, 255, 255, 1) 0%, rgba(240, 249, 255, 1) 100%);

    &::before,
    &::after {
      opacity: 1;
      animation: carMove 2s ease-in-out infinite;
    }
  }

  // 文字和图标容器
  .car-content {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    gap: 15px;

    .dot {
      width: 14px;
      height: 14px;
      background: linear-gradient(135deg, rgb(22, 93, 255), rgb(59, 130, 246));
      border-radius: 50%;
      flex-shrink: 0;
      box-shadow:
        0 0 12px rgba(22, 93, 255, 0.6),
        0 0 20px rgba(22, 93, 255, 0.3);
      animation: dotPulse 2s ease-in-out infinite;
      position: relative;

      &::after {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 6px;
        height: 6px;
        background: white;
        border-radius: 50%;
        opacity: 0.8;
      }
    }

    .text {
      flex: 1;
      font-size: clamp(13px, 2.5vw, 16px);
      color: #1f2937;
      line-height: 1.7;
      font-weight: 600;
      text-shadow: 0 1px 2px rgba(255, 255, 255, 0.8);
    }
  }
}

// 汽车移动动画
@keyframes carMove {
  0%, 100% {
    transform: translateY(-50%) translateX(0);
  }
  50% {
    transform: translateY(-50%) translateX(5px);
  }
}

// 点脉冲动画
@keyframes dotPulse {
  0%, 100% {
    box-shadow: 0 0 12px rgba(22, 93, 255, 0.6), 0 0 20px rgba(22, 93, 255, 0.3);
    transform: scale(1);
  }
  50% {
    box-shadow: 0 0 20px rgba(22, 93, 255, 0.9), 0 0 30px rgba(22, 93, 255, 0.5);
    transform: scale(1.15);
  }
}

// 变体：鲜艳色彩版本（用于重点强调）
.car-outline-bg--vibrant {
  @extend .car-outline-bg;

  background: linear-gradient(135deg, #fff5f5 0%, #ffe4e6 100%);
  border: 2px solid rgba(239, 68, 68, 0.3);

  &::before {
    background: linear-gradient(135deg, rgba(239, 68, 68, 0.3), rgba(248, 113, 113, 0.3));
  }

  &::after {
    background: linear-gradient(135deg, rgba(251, 191, 36, 0.3), rgba(245, 158, 11, 0.3));
  }

  .dot {
    background: linear-gradient(135deg, #ef4444, #f87171);
    box-shadow: 0 0 15px rgba(239, 68, 68, 0.7), 0 0 25px rgba(239, 68, 68, 0.4);
  }
}
```

##### 6.2.2 在组件中使用

```vue
<template>
  <!-- 普通版本 -->
  <div class="car-outline-bg">
    <div class="car-content">
      <div class="dot"></div>
      <div class="text">规模以上工业增加值增长52%</div>
    </div>
  </div>

  <!-- 鲜艳版本（重点强调） -->
  <div class="car-outline-bg car-outline-bg--vibrant">
    <div class="car-content">
      <div class="dot"></div>
      <div class="text">2024年：地区生产总值242.15亿元，同比增长74.2%</div>
    </div>
  </div>
</template>
```

##### 6.2.3 替代方案：使用汽车图标

如果 clip-path 的汽车轮廓不够明显，可以使用 emoji 或 icon：

```scss
.car-outline-bg {
  // ... 保留原有样式

  // 使用汽车 emoji 作为背景装饰
  &::before {
    content: '🚗';
    font-size: 60px;
    opacity: 0.08;
    position: absolute;
    top: 50%;
    left: 10px;
    transform: translateY(-50%) rotate(-5deg);
  }

  &::after {
    content: '🏎️';
    font-size: 50px;
    opacity: 0.08;
    position: absolute;
    top: 50%;
    right: 10px;
    transform: translateY(-50%) rotate(5deg);
  }
}
```

---

## 📊 实施计划

### 阶段一：基础重构（预计12小时）

**任务：**
1. 创建 `useTypewriter` 组合式函数
2. 创建汽车轮廓全局样式
3. 板块2竖向时间轴改造
4. 板块4图文结构调整

**验收标准：**
- ✅ 打字机效果流畅，速度可配置
- ✅ 汽车轮廓样式在多个板块复用
- ✅ 板块2时间轴在手机端一屏完整展示
- ✅ 板块4小标题与图片正确关联

---

### 阶段二：复杂功能实现（预计16小时）

**任务：**
1. 板块1开屏动画4阶段实现
2. 板块3柱状图数据重构
3. 全局文字打字机效果应用

**验收标准：**
- ✅ 板块1开屏动画流畅，4个阶段过渡自然
- ✅ 板块3三个柱状图正确展示，数据准确
- ✅ 柱子逐条出现动画流畅
- ✅ 所有板块文字采用打字机效果
- ✅ 动画协调良好，无重叠冲突

---

### 阶段三：测试与优化（预计8小时）

**任务：**
1. 真机测试（iOS/Android）
2. 性能优化（动画帧率）
3. 细节调整（字体大小、间距等）
4. 图片资源替换

**验收标准：**
- ✅ iPhone/Android 手机测试通过
- ✅ 所有动画保持 60fps
- ✅ 字体大小在各尺寸屏幕上合适
- ✅ 所有占位图替换为真实图片

---

## 🛠 技术难点与解决方案

### 难点1：板块1多阶段动画状态管理

**问题：**
- 4个阶段的状态切换
- 轮播计时与阶段切换的协调
- 用户点击按钮提前进入的处理

**解决方案：**
```typescript
// 使用状态机模式
const currentStage = ref<1 | 2 | 3 | 4>(1)
const canSkip = ref(false) // 是否允许跳过

// 阶段切换控制器
const advanceStage = () => {
  if (currentStage.value < 4) {
    currentStage.value++
  }
}

// 用户点击"跳过"
const skipIntro = () => {
  if (canSkip.value) {
    currentStage.value = 4
  }
}

// 在阶段3启用跳过功能
setTimeout(() => {
  canSkip.value = true
}, 12000)
```

---

### 难点2：打字机效果性能优化

**问题：**
- 同时多个打字机会导致卡顿
- 长文本打字机耗时过长

**解决方案：**
```typescript
// 1. 使用 requestAnimationFrame 替代 setInterval
const typeText = (text: string, callback: Function) => {
  let currentIndex = 0
  let lastTime = Date.now()

  const animate = () => {
    const now = Date.now()
    const delta = now - lastTime

    if (delta >= speed) {
      callback(text.slice(0, currentIndex))
      currentIndex++
      lastTime = now
    }

    if (currentIndex <= text.length) {
      requestAnimationFrame(animate)
    }
  }

  requestAnimationFrame(animate)
}

// 2. 长文本使用更快的速度
const getSpeed = (textLength: number) => {
  if (textLength > 100) return 20 // 快速
  if (textLength > 50) return 30 // 中速
  return 50 // 慢速
}
```

---

### 难点3：柱状图动画与数据同步

**问题：**
- 柱子高度变化需要平滑过渡
- 数字滚动需要与柱子高度同步

**解决方案：**
```typescript
// 使用 GSAP 的 onUpdate 回调
import { gsap } from 'gsap'

const animateBar = (barElement: HTMLElement, targetValue: number) => {
  const obj = { value: 0, height: 0 }

  gsap.to(obj, {
    value: targetValue,
    height: 100,
    duration: 1.5,
    ease: 'power2.out',
    onUpdate: () => {
      // 同步更新柱子高度和数字
      barElement.style.height = obj.height + '%'
      barElement.querySelector('.bar-value')!.textContent = Math.floor(obj.value).toString()
    }
  })
}
```

---

### 难点4：移动端一屏展示适配

**问题：**
- 不同手机屏幕高度差异大
- 需要确保内容不被底部导航遮挡

**解决方案：**
```scss
// 使用 dvh (动态视口高度) 单位
.timeline-vertical {
  max-height: calc(100dvh - 200px); // 减去顶栏和底栏
  overflow-y: auto;

  // 针对小屏手机（iPhone SE）
  @media (max-height: 667px) {
    .timeline-node {
      margin-bottom: 30px; // 减小间距
    }

    .timeline-card {
      .card-image {
        height: 140px; // 缩小图片
      }
    }
  }
}

// 使用 container queries（现代浏览器）
@container (max-height: 700px) {
  .chart-module {
    padding: 15px;

    .chart-bars {
      height: 150px;
    }
  }
}
```

---

## 📝 开发检查清单

### 开发前准备

- [ ] 备份当前代码（git commit）
- [ ] 准备好所有图片资源
- [ ] 确认设计稿细节

### 板块1开发

- [ ] 创建4个阶段的状态管理
- [ ] 实现阶段1欢迎屏动画
- [ ] 实现阶段2大屏轮播（3D Cube效果）
- [ ] 实现阶段3缩小+按钮
- [ ] 实现阶段4内容展示+打字机
- [ ] 测试用户点击按钮交互
- [ ] 测试自动播放时序

### 板块2开发

- [ ] 改造时间轴为竖向布局
- [ ] 调整图片尺寸和卡片样式
- [ ] 实现PC端左右交替布局
- [ ] 实现移动端左对齐布局
- [ ] 添加节点逐个出现动画
- [ ] 测试一屏完整展示效果

### 板块3开发

- [ ] 调整数据结构（三个指标）
- [ ] 创建三模块纵向布局
- [ ] 实现柱子逐条出现动画
- [ ] 添加点击柱子弹窗功能
- [ ] 实现文字打字机效果
- [ ] 测试手机端三模块一屏展示

### 板块4开发

- [ ] 移除独立小标题列表
- [ ] 调整图片+标题结构
- [ ] 补充所有图片资源
- [ ] 测试轮播效果
- [ ] 测试点击弹窗交互

### 全局功能开发

- [ ] 创建 `useTypewriter` 组合式函数
- [ ] 测试打字机速度配置
- [ ] 测试逐行打字机效果
- [ ] 创建汽车轮廓全局样式
- [ ] 应用到所有板块的小标题
- [ ] 测试 hover 动画效果

### 测试与优化

- [ ] 真机测试（iPhone）
- [ ] 真机测试（Android）
- [ ] 测试动画帧率（Chrome DevTools）
- [ ] 测试打字机性能
- [ ] 测试Swiper在不同设备上的表现
- [ ] 检查所有板块的响应式布局
- [ ] 优化图片加载速度

---

## 🚀 后续优化建议

### 性能优化

1. **图片懒加载**
   ```typescript
   // 使用 Intersection Observer API
   const lazyLoadImage = (img: HTMLImageElement) => {
     const observer = new IntersectionObserver((entries) => {
       entries.forEach(entry => {
         if (entry.isIntersecting) {
           img.src = img.dataset.src!
           observer.unobserve(img)
         }
       })
     })
     observer.observe(img)
   }
   ```

2. **动画节流**
   ```typescript
   // 使用 lodash-es 的 throttle
   import { throttle } from 'lodash-es'

   const handleScroll = throttle(() => {
     // 滚动处理逻辑
   }, 100)
   ```

3. **WebP 图片格式**
   ```html
   <picture>
     <source srcset="image.webp" type="image/webp">
     <img src="image.jpg" alt="fallback">
   </picture>
   ```

### 用户体验优化

1. **添加加载骨架屏**
2. **添加"跳过动画"按钮**（板块1开屏）
3. **添加进度指示器**（显示当前第几个板块）
4. **添加返回顶部按钮**

### 可访问性优化

1. **ARIA 标签**
   ```html
   <div role="region" aria-label="时间轴展示">
   <button aria-label="关闭弹窗">×</button>
   ```

2. **键盘导航支持**
   ```typescript
   // Tab 键切换板块
   document.addEventListener('keydown', (e) => {
     if (e.key === 'Tab') {
       // 切换到下一个板块
     }
   })
   ```

---

## 📞 技术支持

### 遇到问题时的排查顺序

1. **检查浏览器控制台报错**
2. **使用 Vue DevTools 检查组件状态**
3. **检查 CSS 样式是否正确应用**
4. **测试不同屏幕尺寸**
5. **清除浏览器缓存后重试**

### 常见问题

**Q1: 打字机效果在某些板块不生效？**
A: 检查是否正确引入 `useTypewriter`，以及 `onMounted` 生命周期是否正确触发。

**Q2: Swiper 3D Cube 效果无法显示？**
A: 确保引入了 `EffectCube` 模块：
```typescript
import { EffectCube } from 'swiper/modules'
import 'swiper/css/effect-cube'
```

**Q3: 汽车轮廓背景在某些浏览器显示异常？**
A: 检查浏览器是否支持 `clip-path`，可使用 emoji 替代方案。

**Q4: 移动端时间轴超出一屏？**
A: 调整 `timeline-node` 的 `margin-bottom` 值，或缩小图片高度。

---

## ✅ 验收标准

### 功能验收

- [ ] 板块1开屏动画流畅，4个阶段正确切换
- [ ] 板块2时间轴竖向展示，手机端一屏完整
- [ ] 板块3三个柱状图数据正确，动画流畅
- [ ] 板块4图文结构正确，标题在图片下方
- [ ] 所有文字采用打字机效果
- [ ] 汽车轮廓背景应用于所有小标题
- [ ] 点击交互正常（弹窗、按钮等）

### 性能验收

- [ ] 首屏加载时间 < 3秒
- [ ] 动画帧率保持 60fps
- [ ] 无明显卡顿和闪烁
- [ ] 内存占用正常（< 150MB）

### 兼容性验收

- [ ] Chrome 最新版测试通过
- [ ] Safari iOS 测试通过
- [ ] 微信内置浏览器测试通过
- [ ] iPhone SE (小屏) 测试通过
- [ ] iPad 横屏测试通过

---

## 📄 总结

本技术方案涵盖了项目优化的所有需求点，采用模块化、组件化的设计思路，确保代码的可维护性和可扩展性。

**核心技术亮点：**
1. ✨ 开屏动画多阶段状态机管理
2. ✨ 打字机效果组合式函数封装
3. ✨ 汽车轮廓全局样式复用
4. ✨ 柱状图逐条动画与数据同步
5. ✨ 响应式布局精细化适配

**预计总工时：36小时**

**建议分3天完成：**
- Day 1: 阶段一基础重构（12h）
- Day 2: 阶段二复杂功能实现（16h）
- Day 3: 阶段三测试与优化（8h）

---

**文档版本：** v2.0
**更新日期：** 2025年
**状态：** ✅ 待实施

如有疑问或需要调整方案，请随时反馈！
